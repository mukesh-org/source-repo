apiVersion: skaffold/v1beta3
kind: Config

build:
  artifacts:
    - image: gcr.io/searce-sandbox/abc
      context: .
      #- image: gcr.io/searce-sandbox/xyz
      #context: .
  kaniko:
    buildContext:
      gcsBucket: searce-sandbox-skaffold_context
    pullSecret: /secrets/gcloud/skaffold-secret.json
    cache: {}

deploy:
   kustomize: {}
