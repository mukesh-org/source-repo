apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
    - image: gcr.io/searce-sandbox/abc
      context: .
      #- image: gcr.io/searce-sandbox/xyz
      #context: .
  kaniko:
    buildContext:
    # localDir: {}
      gcsBucket: searce-sandbox-prow-logs-muk
    pullSecret: /secrets/gcloud/skaffold-secret.json
   # namespace: build-helloworld
    cache: {}

deploy:
   kustomize: {}
